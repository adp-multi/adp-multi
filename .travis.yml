language: haskell

before_install:
  - ghc-pkg list
script:
  - cabal configure --enable-tests --enable-library-coverage && cabal build && run-cabal-test
after_script:
  - cabal install hpc-coveralls
  - hpc-coveralls MainTestSuite
